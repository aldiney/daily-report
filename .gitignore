# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local dev
*.log
.tmp/

# Wazap runtime artifacts (if anything leaks into the repo by mistake)
.wwebjs_auth/
.wwebjs_cache/

# Test snapshots that should NOT be committed (received snapshots, not the baseline)
tests/**/*.received.*

# Session notes: planning, decisions, handoffs, sync logs. Kept locally for
# reference, never published. Each new session adds a dated file here.
/docs/sessions/
